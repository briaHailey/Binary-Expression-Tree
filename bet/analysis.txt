The complexity of the makeEmpty(BinaryNode* & t) function is O(N) because it recursively calls itself once for every node as it traverses the tree, which amounts to N recursive calls, where N is the number of nodes.
